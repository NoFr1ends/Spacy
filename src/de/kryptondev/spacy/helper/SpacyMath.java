
package de.kryptondev.spacy.helper;

public final class SpacyMath {
    
    public static float Acceleration()
    {
        
    }
    
    public float getValue(double x)
    {
      return a * Math.pow(x, potA) + b * Math.pow(x, potB) + c * Math.pow(x, potC);
    }
}